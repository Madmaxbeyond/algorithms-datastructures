function charCount(str) {
    // make object to return at end
    // loop over string, for each character...
        // if the chaar is a number/letter AND is a key object, add one to count
        // if char is number/letter AND not an object, add it to the object and set value to 1
        // if char is something else (space, period, etc.) don't do anything to it
    // return object at end
}